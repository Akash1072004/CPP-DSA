#include<iostream>
#include<string>
using namespace std;
int main(){
    int x;
    cin>>x;
    string s = to_string(x);
    cout<<s.size()<<endl;
    cout<<s.length();
    //find number of digits without using loops...
}